---
pagination:
    data: huchteman.photos
    size: 1
    alias: photo
permalink: "photos/{{ photo.smugmug_slug | slug }}/"
tags:
    - photo
---
{% extends 'layouts/base.njk' %}
{% set pageType = 'photo' %}

{# Intro content #}
{% set introSummary %}
<p class="[ intro__meta ] [ text-500 leading-tight ]">
    {% if date %}
        <time datetime="{{ date | w3DateFilter }}" class="dt-published">{{ date | dateFilter }}</time>
    {% endif %}

</p>
{% endset %}

{% block content %}
    <main id="main-content" tabindex="-1">
        <article class="[ post ] [ h-entry ] nointro">
            <h1 class="[ intro__heading{{ ' intro__heading--compact' if page.url === '/' }} ] [ md:text-700 leading-tight ] [ p-name ]">
            {{photo.title}} </h1>
            <div class="[ post__body ] [ inner-wrapper ] [ leading-loose pad-top-900 {{ 'pad-bottom-900' if not site.enableThirdPartyComments }} text-500 ] [ sf-flow ] [ e-content ]">
                {{ content | safe }}

                <img src="https://bradvsbrad.s3.amazonaws.com/photos/ar/{{ photo.filename_lg }}" class="hero-photo" alt="" />
                <p>{{ photo.caption}}</p>


            {% if tags %}
                <footer class="[ post__footer ] [ pad-top-500 pad-bottom-500 ]">
                    <div class="inner-wrapper">
                        <div class="[ nav ] [ box-flex align-center ]">
                            <h2 class="font-base text-600 weight-mid">Filed under</h2>
                            <ul class="[ nav__list ] [ box-flex align-center pad-left-400 ] [ p-category ]">
                                {% for item in tags %}
                                    <li class="nav__item">
                                        <a href="/tags/{{ item }}">{{ item }}</a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </footer>
            {% endif %}
        </article>
    </main>
{% endblock %}

{{ content | safe }}
